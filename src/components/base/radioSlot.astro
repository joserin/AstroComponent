---
interface Props {
    title:string;
    class?: string;
    [x: string]: any;
}

const defaulClass='p-5 gap-3';

const { title, body, class: className, ...rest } = Astro.props;
---

<div {...rest} class:list={[className, defaulClass]}>
    <slot/>
</div>